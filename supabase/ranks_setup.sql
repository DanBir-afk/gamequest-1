-- ============================================================
-- GameQuest — счётчик пройденных квестов (для значков рангов)
-- Запусти в Supabase: Dashboard → SQL Editor → New query → Run
-- ============================================================

-- Сколько квестов прошёл игрок. Нужно, чтобы на профиле другого
-- пользователя правильно подсвечивались полученные ранги.
alter table public.profiles
  add column if not exists quests_completed int not null default 0;
